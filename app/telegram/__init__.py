"""Telegram-related bot components (handlers, UI helpers, sending).

This package intentionally contains no runtime entrypoints. The public entrypoint
remains `main.py` and `app.bot` re-exports for backward compatibility.
"""

